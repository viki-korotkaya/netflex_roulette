import styled from "styled-components";
import { red, small, smallWeight } from "styles/global_varables";

export const ErrorSpanStyled = styled.span`
  display: block;
  color: ${red};
  font-size: ${small};
  font-weight: ${smallWeight};
`;
